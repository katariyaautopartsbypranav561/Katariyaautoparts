// Firebase stub for local development.
// Real Firebase credentials will be added later when deploying to production.
// Auth is now handled locally via localStorage in AuthContext.jsx

export const auth = null;
export default null;
